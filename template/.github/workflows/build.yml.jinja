name: Build post

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  execute:
    runs-on: ubuntu-latest
    concurrency: post-{% raw %}${{ github.ref }}{% endraw %}
    steps:
      - uses: adrn-blog/execute-post@main
        with:
          python-version: "{{python_version}}"
